workflows:
  development-build:
    name: EAR TRIP - Development Build
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      xcode: latest
      vars:
        XCODE_PROJECT: "EARTrip.xcodeproj"
        XCODE_SCHEME: "EARTrip"
    triggering:
      events:
        - push
        - pull_request
      branch_patterns:
        - pattern: "*"
          include: true
          source: true
    scripts:
      - name: Inspect Apple toolchain
        script: |
          set -e
          xcode-select -p
          xcodebuild -version
          swift --version
          xcrun simctl list runtimes
      - name: Install XcodeGen
        script: |
          if ! command -v xcodegen >/dev/null; then
            brew install xcodegen
          fi
      - name: Generate Xcode project
        script: xcodegen generate --spec project.yml
      - name: Verify generated project and shared scheme
        script: |
          set -e
          test -d "$XCODE_PROJECT"
          test -f "$XCODE_PROJECT/xcshareddata/xcschemes/$XCODE_SCHEME.xcscheme"
          xcodebuild -project "$XCODE_PROJECT" -list
      - name: Build unsigned simulator app
        script: |
          set -o pipefail
          xcodebuild build \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            -configuration Debug \
            -derivedDataPath "$CM_BUILD_DIR/build/DerivedData" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO | tee "$CM_BUILD_DIR/xcodebuild-build.log"
      - name: Run unit tests
        script: |
          set -o pipefail
          SIMULATOR_ID=$(xcrun simctl list devices available -j | jq -r '[.devices[][] | select(.isAvailable == true and (.name | startswith("iPhone")))][0].udid')
          if [ -z "$SIMULATOR_ID" ] || [ "$SIMULATOR_ID" = "null" ]; then
            echo "No available iPhone simulator was found."
            exit 1
          fi
          echo "Using simulator: $SIMULATOR_ID"
          xcodebuild test \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -sdk iphonesimulator \
            -destination "platform=iOS Simulator,id=$SIMULATOR_ID" \
            -configuration Debug \
            -derivedDataPath "$CM_BUILD_DIR/build/DerivedData" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO | tee "$CM_BUILD_DIR/xcodebuild-test.log"
    artifacts:
      - xcodebuild-*.log
      - build/DerivedData/**/Build/**/*.app
      - build/DerivedData/**/Logs/Test/*.xcresult

  testflight-build:
    name: EAR TRIP - TestFlight
    instance_type: mac_mini_m2
    max_build_duration: 90
    integrations:
      app_store_connect: EARTRIP_ASC
    environment:
      xcode: latest
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.eartrip.app
      vars:
        XCODE_PROJECT: "EARTrip.xcodeproj"
        XCODE_SCHEME: "EARTrip"
        BUNDLE_ID: "com.eartrip.app"
        MARKETING_VERSION: "0.1.0"
    scripts:
      - name: Inspect Apple toolchain
        script: |
          set -e
          xcode-select -p
          xcodebuild -version
          swift --version
      - name: Install XcodeGen
        script: |
          if ! command -v xcodegen >/dev/null; then
            brew install xcodegen
          fi
          xcodegen --version
      - name: Generate Xcode project
        script: xcodegen generate --spec project.yml
      - name: Verify generated project and shared scheme
        script: |
          set -e
          test -d "$XCODE_PROJECT"
          test -f "$XCODE_PROJECT/xcshareddata/xcschemes/$XCODE_SCHEME.xcscheme"
          xcodebuild -project "$XCODE_PROJECT" -list
      - name: Validate TestFlight prerequisites
        script: |
          set -e
          APP_ICON_SET="$CM_BUILD_DIR/EARTrip/Resources/Assets.xcassets/AppIcon.appiconset"
          APP_ICON_FILE=$(jq -r '.images[] | select(.idiom == "universal" and .platform == "ios" and .size == "1024x1024") | .filename // empty' "$APP_ICON_SET/Contents.json")
          if [ -z "$APP_ICON_FILE" ] || [ ! -f "$APP_ICON_SET/$APP_ICON_FILE" ]; then
            echo "A production 1024x1024 App Icon is required before TestFlight upload."
            echo "Add the PNG to $APP_ICON_SET and set its filename in Contents.json."
            exit 1
          fi

          BUILD_SETTINGS=$(xcodebuild \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME" \
            -configuration Release \
            -showBuildSettings)
          echo "$BUILD_SETTINGS" | grep -q "PRODUCT_BUNDLE_IDENTIFIER = $BUNDLE_ID"
          echo "$BUILD_SETTINGS" | grep -q "MARKETING_VERSION = $MARKETING_VERSION"
          echo "$BUILD_SETTINGS" | grep -q "ASSETCATALOG_COMPILER_APPICON_NAME = AppIcon"
      - name: Set CI build number
        script: |
          set -e
          agvtool new-version -all "$CM_BUILD_NUMBER"
          test "$(agvtool what-version -terse)" = "$CM_BUILD_NUMBER"
      - name: Apply App Store signing profile
        script: xcode-project use-profiles
      - name: Build signed IPA
        script: |
          set -e
          xcode-project build-ipa \
            --project "$CM_BUILD_DIR/$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
